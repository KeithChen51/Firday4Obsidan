# Friday Update 更新日志

这里记录 F.R.I.D.A.Y 面向用户的版本变化，按版本倒序排列。

## 0.2.9
发布日期：2026-04-24

- `F.R.I.D.A.Y/` 现在是官方频道内容在 vault 里的固定位置。官方栏目会以一级文件夹或一级 Markdown 文件出现，不再和插件配置、运行记录混在一起。
- 官方内容可以单独更新。你可以拿到新的官方栏目和文档，不必等插件本体发新版本。
- 设置页新增“订阅频道”，替代原来的 slash commands 入口。当前先支持“官方频道”，后续会继续扩展其他来源。
- 新出现的官方栏目默认订阅；如果你不需要某个栏目，可以在“订阅频道”里关闭。
- 如果旧 vault 的 `F.R.I.D.A.Y/` 里还放着历史项目、个人目录或其他未知内容，Friday 会先保护这些内容：可以刷新栏目列表，但不会自动覆盖或删除，直到你迁移、清理或确认接管。
- 插件运行记录、步骤追踪和配置镜像不再铺在 `F.R.I.D.A.Y/` 根目录下，vault 里看到的官方频道会更干净。
- “订阅频道”会在缺少 Git、Git 用户名或 Git 令牌时直接提示，并提供跳转到“基础配置”的按钮。
- 官方内容刷新和插件自动更新不再要求填写 Git 邮箱。配置好可读取发布内容的凭据后，就可以检查和更新。
- 官方栏目下线或改名后，本地旧内容的清理更可靠，不会再被当成需要手工处理的历史内容卡住。
- 中文栏目名和包含特殊字符的栏目名现在可以保持独立订阅状态，不容易出现开关串到另一个栏目的情况。
- 官方发布流程同步更新，插件更新信息和官方内容更新信息会一起生成，减少发布时漏传文件的风险。

## 0.2.8
发布日期：2026-04-21

- 项目设置页新增“遗留 Friday 根目录内容”面板。升级旧 vault 时，可以先盘点旧的 `项目` / `个人` / `Agents` 目录，再决定导入或清理。
- 项目边界判断更明确。当前文件属于哪个项目、成员数据写到哪里、同步入口指向哪里，都会以已注册的项目路径为准。
- 本地和远端注册项目时必须明确选择 Obsidian 路径，不再自动猜测目标目录，减少项目嵌套和误写入。
- 工作台顶部只保留 `F.R.I.D.A.Y` 字标；用户消息会优先显示你在基础配置里设置的显示名称。
- 基础配置移除了旧的用户 ID 输入项，配置项更贴近日常使用。
- `F.R.I.D.A.Y` 字标调整回 n-neco 风格字体，只影响工作台品牌、聊天消息角色名和设置标题，不会改变普通正文。

## 0.2.7
发布日期：2026-04-21

- 角色和身份设置统一收口到 Soul。设置模型、切换当前角色、进入 Daily Board 对话工作台时，看到的都是同一套角色信息。
- 升级时会把旧 Agent 档案导入为 Soul 定义，并保留旧会话、工具批准规则和模型偏好。
- “来自制作组”栏目完成命名调整：`幕后笔记 · Behind the Build` 改为 `Study with F.R.I.D.A.Y`，`迭代手记 · Changelog` 简化为 `Changelog`。
- 旧的制作组内容路径仍可识别，升级后已有链接不容易断。
- 初始化命令的全局 `FRIDAY.md` 写入位置更稳定，减少多设备或多 vault 使用时的路径混乱。

## 0.2.6
发布日期：2026-04-20

- F.R.I.D.A.Y 回到单角色执行方式，移除了 subagent 相关入口和设置。执行过程更直接，也更容易判断当前是谁在处理任务。
- 新增长期记忆工具 `memory`，可以显式新增、替换或删除需要跨对话保留的信息。
- 长期记忆的位置更固定：全局记忆写入 `F.R.I.D.A.Y/_runtime/memory/global.md`，项目记忆写入当前项目下的 `.friday/memory/project.md`。
- 记忆写入遵循“下一轮可见”规则，当前回答不会被刚写入的记忆临时改写。
- 移除了旧的自动记忆提取方式，长期记忆不会再悄悄分散落盘，后续排查更容易。
- 补齐记忆和 subagent 下线相关的回归测试，降低这些基础行为在后续版本里退回旧逻辑的风险。

## 0.2.5
发布日期：2026-04-19

- 自动更新卡片更清楚：当前版本号右侧的按钮会在“检查更新”和“应用更新”之间切换。
- 自动更新区域减少了重复入口，移除了单独的“更新操作”和“忽略当前版本”。
- “启动时自动检查”现在只负责检查新版本，不再依赖额外的总开关。
- 应用插件更新成功后，会直接提供“立即重启 Obsidian”按钮。
- 创建项目组时，中文和中英混合名称也能生成可用的项目组 ID。
- 修复基础配置里 Git 用户名、Git 邮箱和 Git 令牌输入框首次输入时丢失焦点的问题，连续录入更稳定。
- 内置技能改为从各自目录的 `SKILL.md` 生成，插件里的技能说明会跟源码保持一致。
- `obsidian-cli` 技能补充了 Windows 预检方式，包括 `where obsidian`、`obsidian version`、`obsidian eval` 和 `PATH` 排查。
- 重写 `obsidian-markdown`、`json-canvas`、`obsidian-bases` 这 3 个 Obsidian 内置技能的说明，让实际编辑规则更贴近 Obsidian 用法。
- 技能管理页会在改写过的 Obsidian 内置 skill 旁显示注释图标，方便查看“原版是什么、为什么改、现在怎么用”。
- 新增“来自制作组”栏目，用来整理 `迭代手记`、`幕后笔记`、`从这里开始` 等内容。
- 插件内部更新日志会同步到 `F.R.I.D.A.Y/来自制作组/迭代手记 · Changelog.md`。
- `来自制作组` 的 README、入门内容和幕后笔记会随插件构建并同步到 vault。
- 对话页里的模型选择会直接修改当前角色的模型配置，不再多一层临时的“跟随默认”状态。
- 对话页里的权限模式会直接写回当前工具权限设置，减少会话级和全局级配置不一致。
- 工作进行中也可以提前输入下一条指令并排队，当前任务完成后会自动继续执行。
- 斜杠命令的技能候选更容易读，`/skill xxx` 会显示为“技能：xxx”。

## 0.2.4
发布日期：2026-04-17

- 新增面向用户的插件更新日志源文件，并按版本倒序维护。
- 插件更新后，会把 Friday Update 更新日志同步到 `F.R.I.D.A.Y/Friday Update 更新日志.md`。
- 更新提示现在可以读取当前版本的发布说明。

## 0.2.3
发布日期：2026-04-17

- 设置页结构更集中，基础配置、项目同步和内部更新入口更容易找到。
- 内部更新流程加入本地 Git 环境检测和版本检查。
- 改进同步过程中的冲突处理和状态提示，减少“正在发生什么”看不清的问题。

## 0.2.2
发布日期：2026-04-17

- 优化会话抽屉和工作台细节，日常操作反馈更及时。
- 增补审计记录能力，方便回看近期执行轨迹。

## 0.2.1
发布日期：2026-04-17

- 新增插件自动更新入口，可以在设置页检查新版本并按提示完成更新。
- 继续完善项目同步能力，让项目同步入口和处理流程更一致。
- 改善设置页和项目编辑相关的交互稳定性。

## 0.2.0
发布日期：2026-04-17

- 重构底层执行框架，为后续对话、工具调用和项目同步能力打基础。

## 0.1.0
发布日期：2026-04-07

- 初始版本，包含待办和项目管理相关的 Obsidian 插件能力。
